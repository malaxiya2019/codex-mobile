#!/bin/bash
if ! git rev-parse --is-inside-work-tree >/dev/null 2>&1; then
    echo "❌ 当前目录不是一个 Git 仓库，跳过自动保存"
    exit 1
fi
git add -A
git commit -m "WIP: $(date '+%Y-%m-%d %H:%M')" --allow-empty 2>/dev/null
echo "📝 已保存进度"
